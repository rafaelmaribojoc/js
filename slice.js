var yourTweet = prompt("Compose your tweet:");
var sliceIt = yourTweet.slice(0, 50);
alert(sliceIt);

/* When you use SLICE feature, you should count from (or start) 0 (most programmers do).
   Here we start counting from 2 upto 7(-1). If there's no number that reached seven,
   it will automatically take all of the letters left.

*/
